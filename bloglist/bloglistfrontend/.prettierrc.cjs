// .prettierrc.cjs
module.exports = {
  semi: true, // Add semicolons at the end of statements
  singleQuote: true, // Use single quotes for strings (e.g., 'hello' instead of "hello")
  printWidth: 100, // Wrap lines after 100 characters (adjust as you like)
  tabWidth: 2, // Use 2 spaces for indentation (common in JS/React)
  trailingComma: 'all', // Add trailing commas for better git diffs and refactoring
  arrowParens: 'always', // Always include parentheses around arrow function parameters (e.g., (x) => x)
};
